export function SuggestionPage() {
  return (
    <div className="card">
      <div className="stack">
        <h2 className="pageTitle">Suggestion</h2>
        <div className="muted">Suggestion wiring will be added next (API call + result card).</div>
      </div>
    </div>
  );
}

